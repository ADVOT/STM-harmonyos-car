# 学习记录 — QST"先锋号"鸿蒙智能小车实训

> 按日期记录实训中的理解性收获（原理、机制、踩坑后的"为什么"）

## 2026-08-25

### 1. 软件是怎么"碰到"硬件的：内存映射 IO 与位带

- CPU 控制引脚的本质：GPIO 外设的寄存器映射在内存地址空间（如 GPIOC 输出寄存器 ODR 在 `0x4001100C`），**往这个地址写值 = 引脚立刻输出对应电压**，没有任何中间层。
- `PCout(13)` 这类宏用的是**位带（bit-band）**技术：把寄存器的某一位映射成独立地址，C 代码就能像操作变量一样操作单个引脚。
- 代码与原理图的对应关系是**人为建立**的：原理图网络标号 `FR-WS2812` 接在 PC13 → 代码 `#define DIL PCout(13)` → 所有 `L_` 前缀函数都通过 `DIL` 驱动前灯带。编译器不懂 FR/BA，绑定点只有宏里的引脚编号。

### 2. WS2812 灯带协议：电压 + 时间 = 比特

- 普通 LED：引脚写 1 亮、写 0 灭，不需要时序。
- WS2812 是"智能"灯珠，内置控制芯片**量高电平脉冲宽度**解码：高 400ns/低 850ns = 比特 0，高 850ns/低 400ns = 比特 1，拉低 >50µs = 一帧结束锁存显示。本质是数字版摩斯密码。
- 24 位 = 一颗灯的颜色，顺序是 **GRB**（不是 RGB），高位先发。
- 级联原理：每颗灯截取数据流前 24 位，其余转发给下一颗，所以 6 颗灯发 6×24 位各取所需。
- 代码用纯软件"位拍打"（`__NOP()` 数指令周期凑纳秒延时）实现时序；代价是刷新期间若被中断打断可能花屏一帧（下一帧自愈）。正式方案应用 PWM+DMA。

### 3. 一个值得记住的设计模式：缓冲-刷新分离

- `L_ws2812_rgb()` 只写内存数组（"显存"），`L_ws2812_refresh()` 才统一发送（"刷屏"）。好处：6 颗灯一次性同步更新，不会一颗一颗闪。

### 4. 裸机上的多任务手法：状态机 + 非阻塞单帧

- 原版 `L_runingled()` 有 `while(1)` 死循环，进去后串口命令再也收不到。
- 改造方法：灯效函数每调用一次只播**一帧**，动画进度用 `static` 局部变量记忆（只初始化一次、跨调用保值），主循环每圈播一帧。这样新命令随时能打断切换。
- 串口中断 → 主循环的交接用"信箱标志"`USART_RX_STA`：中断置 1，主循环处理后**必须清零**，否则是一次性开关。
- 命令识别是逐字节前缀匹配状态机：每来一字节与命令表 `strncmp`，收窄范围；完整命中置标志；失配则丢弃并对齐到可能的新命令首字符。

### 5. STM32 与 Hi3861 的区别（纠正一个错误直觉）

- **不是"STM32 只有一个线程"**——线程是软件概念。STM32 也能跑 RTOS 多线程，Hi3861 也能裸跑。
- 实际区别在当前软件形态：STM32 侧裸机（主循环轮询 + 中断，我改的模式分发就是手动实现的简易调度）；Hi3861 侧跑 LiteOS-M，每功能一个线程（`osThreadNew`），线程 `usleep` 不堵别人——和裸机 `delay_ms` 堵死 CPU 是根本区别。
- 入口也不同：STM32 裸机是 `main()` + `while(1)`；OpenHarmony 用 `SYS_RUN(入口函数)` 宏注册，内核启动后回调，入口里起线程。
- 双核分工的原因：STM32 做电机闭环要微秒级确定性（裸机最可靠）；Hi3861 管 WiFi/多传感器/云通信，并发事件多，必须 RTOS。

### 6. 烧录波特率 vs 程序波特率

- HiBurn 烧录（921600）对接的是芯片 ROM 里的 bootloader，与应用程序的串口配置无关；UartAssist 看打印（115200）才需要和应用里的 UART 初始化一致。两个阶段串口两端是不同的"主人"。
- 波特率 = 每秒传输的比特数（串口的"语速"）：每字节实际占 10 bit（含起始/停止位），115200 ≈ 11.5KB/s，921600 ≈ 92KB/s。收发双方必须设一致，否则乱码。
- **速度拿信号质量换**：异步串口没有时钟线，靠双方各自打拍子采样。921600 时每 bit 仅 ~1.1µs（115200 时 ~8.7µs），bit 越窄越怕线长/干扰/电压抖动。烧录 `load fail + CRC 错` = 线上错了 bit。

### 7. `python build.py wifiiot` 的编译链路

- `build.py` 只是薄入口（68 行）：解析命令行，把第一个位置参数当**产品名**，转发给 `build/lite/compile.py`；真正逻辑都在 `build/lite/` 里。
- 产品配置 `build/lite/product/wifiiot.json` 是核心：声明 `board=hi3861v100`、`kernel=liteos_riscv`、编译器 `riscv32-unknown-elf-gcc`，以及要编译的子系统/组件清单（applications、iot_hardware、wifi、startup…）。
- 实际执行两条命令：**gn gen**（GN 描述 + 产品参数 → 生成 `build.ninja`）→ **ninja**（调交叉编译器真正编译）。gn/ninja/gcc 三者缺一不可，这就是任务3/4 装环境的意义。
- 注意 `check_build` 每次先**删掉 `out/wifiiot/` 再重建**，所以每次都是全量编译，跑一轮几分钟属正常；成功标志是 `ohos wifiiot build success!`。
- 产物 `out/wifiiot/Hi3861_wifiiot_app_allinone.bin` 就是 HiBurn 烧录要的那个文件——**编译和烧录在这个文件上接头**。

### 8. 全车外设清单（原理图 `docx/Schematic_QST-鸿蒙小车.pdf` 核实）

ST-Link/SWD 只能看到 STM32 芯片本身，**没有"扫描传感器"的功能**——外设清单只能从原理图确认：

- **Hi3861 侧**：红外对管 TCRT5000×2（IO13 左 / IO14 右，循迹）、超声波（IO07=TRIG / IO08=ECHO，测距）、SHT20 温湿度（I2C）、AP3216C 光照（I2C）、舵机（IO02 PWM）、OLED 0.91寸（I2C）、蓝牙 JDY-16（UART）、WiFi（内置）、LED（IO06）、按键 KEY1
- **STM32 侧**：电机×2（L9110S：左 PB7/PB14、右 PB13/PB6）、编码器×2（左 PA0/PA1、右 PA6/PA7）、NFC（UART2 PA2/PA3）、WS2812 灯带×2（前 PC13 / 后 PC14 各 6 颗）
- **两条暗线**：① Hi3861 IO11/IO12 ↔ STM32 PA10/PA9 = 双核 UART 互联（任务24 走这）；② OLED/SHT20/AP3216C 三者**并联共用同一条 I2C 总线**
- CH340（Type-C）经 SW2 开关选择连 Hi3861 还是 STM32；ST-Link 是独立另一路 USB，只管下载/调试 STM32，不产生 COM 口

### 9. 任务5 Hello World 的代码结构与"接线"机制

- 入口：`app/1.0_Hello_World/helloworld.c` 用 `SYS_RUN(DemoSdkMain)` 注册 → 内核启动后回调 → 调 `DemoSdkEntry()`（实现在 `domains/iot/link/libbuild/demosdk.c`）。
- 打印链：`DemoSdkEntry` 先打印 `it is demosdk entry.`，再**创建一个名为 demotask 的线程**（这正是任务5 要学的"任务管理 API"），线程里每秒打印 `it is demo biz: hello world.`。所以验收标志 = 串口看到这两行、第二行每秒重复。
- **关键机制：新建一个代码目录 ≠ 会被编译**。必须在 `app/BUILD.gn` 的 `features` 里登记 `"目录名:目标名"`（目标名是 BUILD.gn 里 `static_library("...")` 的名字，如 `example_demolink`），gn/ninja 才知道把它链进固件。这就是为什么 15:52 编出的 bin 里没有 Hello World——代码 16:16 才建，且 features 还没接。
- 验证 bin 内容的小技巧：`strings 固件.bin | grep -i hello`，能直接看出某个打印串编没编进去。
- **两级 BUILD.gn 的角色**：组件级（如 `app/BUILD.gn`）是"装配清单"——`lite_component { features = [...] }` 只登记模块、不编译代码；模块级（如 `1.0_Hello_World/BUILD.gn`）是"加工图纸"——`static_library { sources/include_dirs }` 才真正描述怎么编。加上产品层 `wifiiot.json`，共三层：产品 → 组件 → 模块。
- features 条目写法：目标名 = 目录名时只写目录名（如 `"startup"`）；不同时必须写全称 `"目录:目标"`（如 `"1.0_Hello_World:example_demolink"`）。
- 顺带搞懂：`#include "xxx"` 先搜当前 .c 所在目录再走系统路径，`#include <xxx>` 只搜系统路径（编译器内置目录 + `-I` 目录）。惯例：标准库用 `<>`，项目自己的头文件用 `""`；BUILD.gn 的 `include_dirs` 会被 gn 翻成 gcc 的 `-I` 参数，项目头文件就是靠它找到的。
- **编译器找头文件 ≠ 编辑器弹补全**：gn 看 BUILD.gn 的 `include_dirs`，VSCode 看 C/C++ 插件自己的 `includePath`（两套独立配置）。VSCode 点不出结构体成员 = 它找不到头文件定义，不代表代码错；要提示生效就把头文件目录加进 `C/C++: Edit Configurations` 的 Include path（远程虚拟机绝对路径）。
- `osThreadNew` 的 attr 三个关键成员：`name` / `stack_size`（如 1024）/ `priority`（如 `osPriorityNormal`）；后两个不给（=0）线程创建可能直接失败。任务里延时用 `usleep()`（官方示例同款）。

### 10. C 报错的"报错位置 ≠ 出错位置"

- 现象：编译器/编辑器把错误标在文件末尾的 `}` 上，说"缺语句"，真正的错却在上面十几行的 `if` 行——多了一个 `(` 没配平。
- 原因：编译器读到 `{` 时还以为自己仍在 `if(` 的条件表达式里，后面的代码全被当成表达式错乱解析，直到文件末尾层级对不上才报出来。
- 对策：报错指在莫名其妙的括号/大括号上时，**往前翻找没配平的 `(` 或 `{`**，别在报错行死磕。写完一行条件顺手数一遍开闭括号是最快的自检。

## 2026-08-26

### 1. 多线程的意义：并发 ≠ 加速（单核 Hi3861）

- Hi3861 是单核，任意瞬间只有一个线程在跑；多线程是内核按"抢占式优先级 + 同级时间片轮转"切出来的并发假象。**不会让计算变快**，上下文切换本身还有开销。
- 真正解决三个问题：①**阻塞不冻全局**——线程里 `usleep`/`UartRead` 等待是主动让出 CPU（进入阻塞态），等待时间别的线程照跑；对照裸机 `delay_ms` 是空转占死 CPU，超级循环里所有事全被卡住。②**解耦**——每个功能一个独立 `while(1)`，各写各的。③**响应**——高优先级线程可抢占低优先级，急事急办。
- 建线程三要素：`osThreadNew(入口函数, 传参, attr)`；attr 关键成员 = `stack_size` / `priority` / `name`。`name` 只是调试标签，任意起、不参与调度、不必和 C 函数名一致；内核认线程靠的是返回值 `osThreadId_t`，不是名字。
- 任务6~10 全建立在这套机制上（任务7 三线程互斥锁、任务9 三线程消息队列、任务10 四线程联动），任务5 补成多线程就是给后面铺地基。

### 2. shell 分交互/非交互，决定 PATH 有没有

- 踩坑：非交互 ssh 直跑 `python build.py wifiiot` 连环报 `No option 'riscv32-unknown-elf-gcc_path'` → `Can't find gn`，末尾还打印 usage 伪装成参数错误（真错误在输出第一行）。
- 根因：工具链 PATH 写在 `~/.bashrc` 里，而 `.bashrc` **只有交互 shell 才加载**；`ssh host '命令'` 是非交互 shell，拿不到这五行 export。VSCode Remote 终端恰好是交互 shell，所以之前编译从没踩到。
- 解法：远程直跑时在命令里显式 `export PATH=~/gcc_riscv32/bin:~/ninja:~/gn:~/hc-gen:~/llvm/bin:$PATH` 再调 build.py。

### 3. 线程优先级：怎么设、能填什么、方向别搞反（源码实证）

- **设置两个时机**：创建时填 `attr.priority`；运行中改用 `osThreadSetPriority(id, 新优先级)`（前提是建线程时存住返回值 `osThreadId_t`），读取用 `osThreadGetPriority(id)`。
- `osThreadAttr_t` 是普通结构体，`osThreadNew` **调用那一瞬间**才读它的值：多个线程共用一个 attr 时，在每次 `osThreadNew` 前重新给要改的字段赋值即可（字段赋值顺序无关，时机才有关）；建完后再改 attr 对已建线程无影响。
- **有效区间 9~38**（`osPriorityLow1` ~ `osPriorityAboveNormal6`，`cmsis_liteos2.c` 里有硬校验，超出 `osThreadNew` 直接返回 NULL）。枚举档位 Low=8 / BelowNormal=16 / Normal=24 / AboveNormal=32 只是基准，档内可 +1~+7 微调（`osPriorityNormal1`=25）；课程参考代码直接写裸数字 24/25/26 是一回事。注意这版 SDK 里 `osPriorityHigh`(40) 超范围，用了会创建失败。
- **方向：CMSIS 数字越大优先级越高**。内部映射公式 `losPrio = 31 - (cmsis值 - 8)`（`LOS_PRIORITY_WIN=8`、`OS_TASK_PRIORITY_LOWEST=31`），翻成 LiteOS 的 0~31（0 最高）。例：AboveNormal(32)→LOS 7，Normal(24)→LOS 15。
- 调度规则：高优先级**就绪即抢占**低优先级；同级时间片轮转；低优先级只能等高优先级阻塞/结束。反直觉点：两个都在睡觉的线程改不同优先级看不出区别，优先级要抢 CPU/抢锁时才显现（任务7 优先级翻转就是演示这个）。

### 4. 红外对管测的是"反射强度"，不是颜色（任务6 实测）

- 实测现象：手遮住传感器也打印 black——不是 bug，是原理。TCRT5000 = 红外发射管 + 接收管，接收管量的是**反射回来的红外光强**，模块上的比较器按阈值出电平：反射强 → 高电平（white），反射弱 → 低电平（black）。
- "识别黑色"只是特例之一：黑色吸光所以反射弱。但**没有反射面**（手挡住光路、小车架空离地太远）同样反射弱 → 一样归 black。所以 black 的真实含义 = "反射弱"，可能是黑线 / 悬空 / 被遮挡。
- 环境光的影响：阳光、白炽灯含红外成分会有干扰，LED/荧光灯光影响小；主变量永远是传感器正对的那块反射面，不是"环境亮不亮"。
- 工程推论：循迹要保证 ①传感器离地高度在量程内（TCRT5000 就几厘米）②地面黑白对比明显；把车架空测试时两轮都读 black 属预期，别误判成故障。

### 5. 软件定时器的三个硬规则（任务6 作业）

- **回调里不能阻塞/睡眠**：所有软件定时器的回调都由系统**唯一一个**"定时器任务"统一执行（Tick 中断只负责记录到期，定时器任务被唤醒后挨个调回调）。在某个回调里 `sleep(3)`，卡住的是**全部**定时器——50ms 的红外扫描会被拖成 3 秒一跳。回调要短平快；要干慢活，发给普通线程去干。
- **参数在调用瞬间被拷走**：`osTimerStart(id, ticks)` 的 ticks 在调用那一刻就拷进了内核，事后修改那个变量**不影响**已在运行的定时器；要改周期必须再调一次 `osTimerStart`（对运行中的定时器 = 拿新值重新武装）。和 `osThreadNew` 读 attr 是同一个"调用瞬间定格"原理。
- **`50U` 的 U 不是单位**：是 C 语言"无符号整数字面量"后缀（unsigned int），防止溢出/警告的写法习惯。时间的真正单位是 tick（Hi3861 上 1 tick=10ms），`osTimerStart` 第二参数类型是 `uint32_t`。换算：要 N 毫秒填 N/10，如 3 秒 = `300U`。

### 6. 全局变量、遮蔽陷阱与无符号下溢（任务6 作业）

- **"提全局"不是联动本身**：联动的本质是"一个定时器的回调拿着另一个定时器的 ID 去操作它"；全局化只是把变量的可见范围扩大到所有函数。规则：谁要跨函数碰它，才把它提全局——`timerId` 要提是因为 `HelloQstCallback` 里要重新武装它；`timerId2` 只在 TCRTTask 里启动一次、没别处碰，就保持局部。全局变量不是越多越好。
- **遮蔽陷阱**：变量提全局后，函数里再写 `类型 名字 = ...` 是**新建同名局部变量**，全局那份还是初值，跨函数操作必然失败（如 `osTimerStart(NULL,...)`）。定义处和赋值处只能有一处带类型。
- **无符号下溢**：`uint32_t` 的 0 再减 1 会回卷成约 42 亿（≈497 天的 ticks）。对周期做 `--` 必须带下限保护（`if(scanTicks>2)`），且改完紧跟 `osTimerStart` 重新武装才生效——作业里这两点都自己写对了。
- `scanTicks--` 减的是 1 个 tick=10ms：效果是"每 3 秒快 10ms"的渐变加速，约 2.4 分钟降到 20ms 下限后稳定。

### 7. Hi3861 的 Flash：2MB、分区表、覆盖式烧录

- 每次编译生成同一个 `Hi3861_wifiiot_app_allinone.bin`，HiBurn 烧录 = **先擦除再整体覆盖**同一分区——芯片里永远只有一份固件，不会随迭代"堆积"。
- 内置 Flash 共 **2MB**（分区表边界正好到 0x200000，`vendor/hisi/hi3861/hi3861/platform/system/partition_table/flash_partition_table.c`）。关键分区：Boot 32K @0x0、**Kernel A（应用固件区）912K @0xD000**、Kernel B 968K @0xF1000（OTA 备份区——A/B 双分区升级：新固件先写备份区、验证能跑再切换，防变砖）、文件系统 44K、NV 参数区若干。
- 当前 bin ≈760KB，其中 95% 是系统本体（LiteOS 内核 + WiFi 协议栈 + 驱动库），应用代码只占几 KB；任务6~10 每任务替换前一个（`app/BUILD.gn` 切 feature），体积基本不涨，离 912K 上限还很远。
- 分清两种"存储"：固件存 Flash（2MB，断电保留）；线程栈（`stack_size`）、全局变量用的是 SRAM 运行内存，是另一份资源。

### 8. 互斥锁和线程调度是两套机制，不矛盾（任务7）

- 线程调度管"谁在用 CPU"：线程等待（`osDelay`/等 IO/**等锁**）时内核把 CPU 切给别人。互斥锁管"谁在碰某个资源"：两者解决的是不同问题。
- 为什么舵机要锁：`set_angle()` 是软件位拍打发 20ms 脉冲，两个线程的脉冲一旦交织，舵机收到的就是乱码信号。内核不知道"舵机转到一半"，调度器只管时间片，保护资源靠线程自己约定"碰舵机前先持锁"。
- **等锁本身就是一种阻塞态**——锁没有禁止切换，反而制造了新的切换理由。高优先级 thread1 被持锁的 thread3 堵住干等（优先级翻转），恰恰是这套机制在正常工作。
- **锁是君子协定，不是硬件围栏**：持锁期间没有任何硬件机制拦住别人碰舵机——不守规矩的线程不调 `osMutexAcquire` 直接调 `set_angle()` 照样能驱动（波形就乱了）。被堵的只有"守规矩且想碰同一资源"的线程；不碰锁的线程（如 thread2）完全不受影响。所以铁律：**碰共享资源的每一处代码都必须先持锁**，漏一处约定即失效。

### 9. `osThreadNew` 不是 C 关键字，是普通库函数

- C 关键字只有 32 个左右（`if`/`for`/`static`...），`osThreadNew`/`osMutexAcquire`/`osMutexRelease` 全是**函数**：CMSIS-RTOS2 标准接口（ARM 定的规范），实现是 LiteOS-M 内核写的，源码在 `kernel/liteos_m/components/cmsis/2.0/cmsis_liteos2.c`（优先级 9~38 硬校验就在里面）。
- 分层记：`printf`=C 标准库；`osXxx`=ARM RTOS 标准 API（LiteOS 实现）；`GpioSetOutputVal`/`hi_udelay`=海思芯片 SDK。都是"别人写好、编译时链进固件的函数"，区别只在作者。
- 调用链路和任务5 的 BUILD.gn 机制同一回事：`#include "cmsis_os2.h"` 拿声明 → `include_dirs` 找头文件 → 内核组件编进固件。没有魔法，全是链接。

### 10. STM32 电机控制链路：PWM 调的不是电压，是时间比例（任务21）

- **模块接口设计**：`motor.h` = 契约——宏给硬件起名（`AIN=PBout(13)` 位带写方向脚；`PWMA=TIM4->CCR1` 直指比较寄存器 `0x40000834`）+ 声明三函数。`main.c` 只需 `PWM_Init` 一次 + `Set_Pwm` 调速，不知道 TIM4 的存在——细节全关在 motor.c 里。
- **STM32 铁律：先开时钟再配置**。每个外设有独立时钟开关（默认关闭省电），不开 `RCC_APBxPeriphClockCmd`，写寄存器被硬件无视。
- **复用推挽（AF_PP）= 引脚控制权移交**：普通输出由软件写 ODR 决定电平；配成 AF_PP 后 PB6/PB7 归 TIM4 硬件自动翻转——PWM 不占 CPU 的关键就在这步。
- **TIM4 PWM 机制**：CNT 从 0 数到 ARR 归零循环（72M/(PSC+1)=7.2MHz 计数，7200 个数 = 1ms = 1kHz）；比较器每拍判 `CNT<CCR?高:低`（PWM1 模式）→ 占空比 = CCR/7200。CPU 写完寄存器就自由了。
- **反转的反相写法**：方向脚置 1 后 L9110S 在 PWM **低电平期间**驱动电机，有效时间 = (7200-CCR)，故 `CCR = 7199-|速度|` 把占空比翻过来。
- **"调电压"的真相**：引脚永远只有 0/3.3V，电机两端永远只有 0/电池电压——调的是**占空比**。线圈是电感、电流不能突变，1kHz 通断快到来不及反应，等效为平均电压 ≈ 占空比 × 电池电压（35% × 7.4V ≈ 2.6V）。

### 11. 没有陀螺仪怎么知道朝向：差速里程计（任务22 引申）

- 车上没有陀螺仪/磁力计，唯一的"朝向传感器"是两个编码器。原理：同一周期内两轮走过距离相等 → 朝向没变；不等 → 必然转了弯。几何关系 `Δθ = (S右 − S左) / 轮距L`，每个测速周期累加一次即得**相对出发点的朝向**。
- 两个固有局限：①只有**相对**朝向（相对起点），感知不到绝对方位；②会**漂移**——打滑、地面接缝、计数误差全部累进角度，跑得越久越不准。短距离直线够用，长时间要靠绝对参照纠偏（超声波贴墙，墙不漂）。
- 一句话记法：**两编码器计数差 = 朝向的表**，差为 0 = 直行。测量（本条）+ 修正（下条）= 闭环的两半。

### 12. 直线走歪了怎么回调：串级闭环（外环纠偏 + 内环调速）

- 先认清病根：两边给同样 PWM ≠ 同样转速（电机特性/摩擦/接地差异），**开环必歪**，所以必须闭环。
- **内环**（任务23 本体）：每轮一个速度 PID——设定速度 vs 编码器实测 → 修 PWM，保证"要多少速度给多少速度"。
- **外环**（直线纠偏）：`偏航误差 = S左累计 − S右累计`，过 PID（入门先只用 P）得修正量，**差动叠加**到两轮设定：`左目标 = 基准 − 修正，右目标 = 基准 + 修正`。左轮走多了就压左抬右，把里程差追平。
- 回调的正常形态是车画**缓 S 形**回到原航向附近，不是直线瞬间掰回来。
- 三条工程纪律：修正量**限幅**（纠太猛会来回摆=振荡）；**先调内环再调外环**（内环不稳外环白调）；纠偏周期复用 100ms 测速节拍。

### 13. 定时器 ≠ 通道：一个定时器是"一个时基 + 4 个通道"

- 层级：定时器有时基（CNT/PSC/ARR，决定频率）和 4 个通道（CH1~CH4，各有独立 CCR，决定各自占空比/输出脚）。**同频率 + 独立占空比**的需求，一个定时器就能带 4 路。
- 本车定时器分配：TIM4 一个定时器的 CH1/CH2 带双电机 PWM（1kHz 同频、占空比独立）；编码器模式把 CH1/CH2 成对吃掉（A/B 两相），整个定时器被独占，故左右各一个：TIM2、TIM3。TIM1 是高级定时器（互补输出/死区/刹车，给 H 桥和无刷准备的），用不上就空着。原则：一个外设能干的活不占两个。
- 芯片实情：STM32F103C8（中密度 64KB）实际只有 **4 个定时器（TIM1~4）**；启动文件里的 TIM5/6/7 中断向量是同族大容量型号共用启动文件的占位，C8 硅片上没有。资料说的"6 个"多半是把内核 SysTick 和 RTC 也算了进去——那两个不是通用定时器。

### 14. PWM 的 0/1 分割是硬件比较器逐拍判出来的

- 机制：计数器时钟 = 72M/(PSC+1) = 7.2MHz（每拍 ≈139ns），CNT 从 0 数到 ARR=7199 归零循环 = 7200 拍一圈 = 1ms。每一拍硬件比较器判一次 `CNT < CCR`：小于输出高、否则输出低（PWM 模式1）。**分割点就是 CCR 这个数**，切换精度 1 拍；占空比 = CCR/7200。软件全程不参与，这就是 PWM 精确且不占 CPU 的根本原因。
- **预装载（preload）是防畸形脉冲的关键**：开了 CCR/ARR preload 后，运行中改占空比不当场生效，等本周期走完才换——避免周期中间改阈值切出半高半低的怪波。这是主循环随时 `Set_Pwm` 都安全的原因。
- 边界：CCR=0 恒低（0%），CCR≥ARR+1 恒高（100%）；反转写 `7199-|x|` 是配合 L9110S"方向脚置1 后低电平期间驱动"的特性（见第10条）。

### 15. tick 是什么：系统的"心跳计数"（任务8 作业）

- tick = 内核定时器按固定频率中断、每次 +1 的计数器，是系统里一切时间的来源：线程延时（`osDelay`）、时间片轮转、软件定时器到期，全按 tick 算账。
- Hi3861 上 **1 tick = 10ms**：`osDelay(200)` = 睡 200 tick = 2s；`osTimerStart` 的 ticks 参数同此。日常其实一直在用，只是没察觉。
- `hi_get_tick()` 读的是从内核启动到此刻的 tick 总数（32 位），打印出来就是"开机多久了"的原始计数，自己 ×10ms 才是秒。同族还有 `hi_get_tick64` / `hi_get_milli_seconds` / `hi_get_us`。
- **层次别混**：tick 是 10ms 粒度的调度心跳；`hi_get_us()` 是微秒级时间戳，另一个来源。任务8 超声波飞行时间就几毫秒，tick 太粗量不了，必须用 `hi_get_us()`。
- 作业设计张力：定时器回调要短平快（第5条），而 `GetDistance()` 正常几毫秒、超时路径上百毫秒，严格说不该直接塞回调里——干净写法是回调只置标志、线程拿到标志再测。作业规模小直接调也能跑，但要意识到这层。

## 2026-08-27

### 1. Hi3861 的 `UartRead` 是阻塞读（反汇编实锤）

- 事故经过：JDY-16 改名工具的响应收集循环里，第一次 `UartRead` 读到模块回的 `OK`，第二次调用时缓冲已空——**线程永久挂死**，后续指令再也发不出。
- 实锤路径（全在编译产物反汇编 `Hi3861_wifiiot_app.asm` 里追到）：`UartRead → HalUartRead → hi_uart_read → uart_read → hi_event_wait`。底层是事件等待：空缓冲时线程挂起，等 UART 接收中断置事件才唤醒。
- **更正任务9 的认知**：当时以为 thread2 是"每秒轮询串口"，实际是**阻塞等待**——`osDelay(100)` 之后阻塞在 `UartRead` 里直到手机真发数据。没暴露是因为另有线程在跑、现象完全正常。"空读返回 0"的防御分支可能从未真正执行过。
- 解法：读前先 `UartIsBufEmpty(id, &empty)` 查空，非空才读。坑中坑：这个函数实现在库里、但 `wifiiot_uart.h` **漏了声明**，要手动 `extern` 补原型。
- 通用教训：**RTOS 驱动 API 的阻塞/非阻塞语义不能凭名字猜**，文档不写就翻实现或做最小实测（发个指令看没数据时返不返回）。

### 2. JDY-16 蓝牙 AT 指令：无参数=查询，跟参数=设置

- 模块只在**蓝牙未连接**时把 UART 输入当 AT 指令解析；一旦连上就是透传模式，AT 原样转发给手机。所以改名只能在断开状态下做。
- 指令必须 `\r\n` 结尾（手册原话"务必加上"）。
- **查询 = `AT+NAME\r\n`（不带参数）**；网帖流传的 `AT+NAME?` 是坑——`?` 被当成名字参数，响应 `+NAME=?` 其实是"已把名字设成 ?"的回显，不是查询结果。JDY 惯例：同一指令，无参数=查询，跟参数=设置。
- 改名 `AT+NAME<名字>`（无等号无空格），响应回显 `+NAME=<新名>` + `OK`；写模块自身 Flash，掉电不丢。
- 架构上注意：JDY-16 接在 Hi3861 的 **UART1**；电脑串口助手经 CH340 连的是 **UART0 调试口**——AT 指令只能由板上程序发，串口助手手动发永远到不了模块。

### 3. 串口通信的编码三坑（一天集齐）

- **全角标点**：手机中文输入法的 `？` 是 UTF-8 三字节 `EF BC 9F`，不是 ASCII 的 `?`（0x3F）。GBK 终端显示成乱码只是表象，实质是字节完全不同——`strncmp(buf,"ready?",6)` 第 6 字节对上 `0xEF` 直接不匹配。教训：匹配用户输入只认字母数字前缀（`"ready"`），别带标点。
- **固件里 printf 中文**：源码 UTF-8 保存，中文字符串按 UTF-8 字节编进固件，GBK 串口助手显示必然乱码（`澶嶄綅涓€娆°`）。规则：**进固件的打印字符串一律英文/ASCII，中文只写注释**。
- **波特率不匹配**：第三种乱码源。本车两个速率别混：CH340 调试口 115200，JDY-16 透传固定 9600。看到乱码先想这三类：编码（UTF-8/GBK）、全角半角、波特率。

### 4. 低速档抖动的病根：静摩擦死区 + 积分 = 极限环（任务23 作业实测）

- 现象：三档轮换（140/280/420 脉冲/100ms）中 280/420 档稳态 ±2 纹丝不动，140 档却在 0~270 之间持续来回甩（周期约 2s），永远稳不住。
- 机制：低速所需 PWM 很小，落进静摩擦死区——轮子卡住不动 → 误差持续灌进积分项 → PWM 憋到突破静摩擦 → 轮子窜出去冲过目标 → 误差反号、PWM 回落 → 又卡死。滞-滑循环，控制里叫**极限环**，是物理现象不是参数没调好。
- 判据：高档稳、低速档周期性震荡 → 先怀疑死区，别急着动 PID 参数。缓解手段有最低 PWM 门槛（前馈补偿）、死区内停止积分等，本任务规模用不上，认得它就行。

### 5. "走直线"的准确含义：航向保持，不只是两轮同速（任务23 外环落地）

- 内环只保证"两轮各自到目标速度"，不保证车不歪——起步/扰动过程中已经偏掉的角度不会自己回来。外环把里程差（=朝向的表，第11条）压回零。
- 实现：`diff = Odo_L − Odo_R`，`corr = KP_YAW × diff`（限幅 ±40），`左目标 = 基准 − corr、右目标 = 基准 + corr`。离散稳定条件 `0 < KP_YAW < 1`（每拍 diff 约乘 (1−2K)），再考虑内环响应延迟取 0.1 起步——串级铁律：**外环必须比内环慢 3~5 倍**，画龙（来回摆）就减半、纠不动就加倍。
- 边界：纯编码器只能保持**相对航向**（起步那一刻的朝向），起步摆斜了就保持斜角走直线；绝对航向必须外部参照（超声波贴墙/视觉，与第11条的漂移问题同根）。
- **落地首跑实测（任务23 往返）**：起步猛给油时两轮挣脱静摩擦有先后（右轮慢半拍）→ 车头先歪 → 外环画缓 S 把**航向**拉回，但 S 形已造成的**横向位移**不在反馈量里、消不掉 → 前进偏右斜线、回程直线、终点偏移一个车身位。**航向回原 ≠ 位置回原**：往返终点偏移 = 前进段纠偏的横向遗产。对策：软启动爬坡（目标每拍只动 20）让挣脱对称+外环低速就有纠偏余地；要彻底消偏移需再加横向位置环（测轮距+积分估计，复杂度上一档，暂缓）。
- 顺带一个编码教训：Keil 工程源码是 GBK 编码，注释里的 `−`（U+2212 数学负号）GBK 没有，iconv 转码直接炸——注释里的数学符号只用 GBK 收录的（× ± → ≈），负号用 ASCII `-`。

### 6. 队友直线成功的启示：直线只要"同速"，内环已够（任务23）

- 队友代码对比（留底 `backups/teammate_user/`）：硬件层（motor/encoder 引脚、TIM 配置）与我们完全一致；控制层 = 同款增量式 KP=7.0/KD=0.003，官方死积分代码原样保留；**没有外环、没有里程计**，方向靠 5 秒时间片翻转。
- 他能走直线的关键：直线的条件是两轮**同速**而非同 PWM——内环积分作用自动给阻力大的轮补 PWM（我们实测地面同速下 L2650/R3400），零静差特性天然补偿机械不对称。
- 推论：我们的外环在地面负载下**反而是振荡嫌疑**——内环变慢后外环相位滞后，架空时 diff±30 的温和猎振到地面可能发散成画龙。教训：**串级里每一环都是潜在不稳定源，不是必需就不加**；用 YAW_CORRECT 开关做 A/B 实验隔离变量，别一上来叠满功能。（后查明偏右真凶是地板斜——环境干扰在传感器模型之外，此条方法论仍成立）

### 7. 超过 5 米的直线：误差分两类，治法完全不同（任务23 延伸）

- **系统误差**（每趟同方向偏、可复现）：头号嫌疑是**两轮有效直径差**——外环只把"脉冲数差"压到 0，而脉冲数相等 ≠ 实际距离相等。直径差 0.5% → 车走半径 ≈轮距/0.005 ≈ 30m 的弧，5 米横向偏 ≈ L²/2R ≈ 0.4m。治法 = **标定**：跑固定里程量横向偏移 → 换算轮径比 → 代码里给单轮目标乘修正系数，一次标定长期有效。
- **随机误差**（打滑、地缝、侧滑）：编码器只认轮子转没转（地板斜那次的教训），纯里程计 = 航位推算（dead reckoning），**误差随距离无界增长，调参调不出有界**——这是物理性质，不是参数问题。次级缓解只有减速（打滑随速度/加速度增长）、外环增益别过大。
- 要有界必须引**绝对参照**：超声波贴墙（墙不漂）、红外循线、视觉。本车绝对传感器全挂 Hi3861、电机在 STM32 → 长距直线在架构上 = **任务24 双核 UART 打通后**，Hi3861 周期性下发横向修正，STM32 双环保持短距精确。环路层级：内环（速度，100ms）→ 外环（航向，100ms）→ 参照环（位置，更慢），一级比一级慢，串级纪律同第12条。
- 动手顺序：先平地跑 5m **定量**（没测别先工程化）→ 同向固定偏就标定轮径比 → 随机偏/更长距离再上参照环。
