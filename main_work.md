# main_work — 已入库代码工作记录

> 只记录**已提交到本仓库的代码**中所含的工作；未入库的本地环境、文档、笔记不在此列。

## 仓库内容概览

- `print_io/`：STM32F103 Keil MDK 工程（标准外设库），QST"先锋号"从核心（运动执行侧）固件的开发基础
- `motor_pwm/`：任务21-23 合体工程（print_io 副本 + 自写电机驱动/编码器模块），TIM4 双路 PWM + TIM2/3 编码器测速 + 增量式 PID 速度闭环 + 行程状态机
- `README.md`：GitHub 仓库展示页（项目简介 / 架构 / 进度）
- `学习记录.md`：实训过程中的理解性收获（原理、机制、踩坑分析）

## print_io 工程（对应任务18/19）

### 1. 工程模板实机验证（任务18）

- MDK5 + STM32F1xx_DFP 环境编译模板工程通过（0 Error）
- ST-Link 实机下载验证：Erase / Program / Verify 全通过，跑马灯正常显示

### 2. WS2812 双灯带驱动（模板自带，已吃透）

- 前灯带 PC13 / 后灯带 PC14，各 6 颗 WS2812
- 单线归零码时序用软件"位拍打"实现（`__NOP()` 凑纳秒延时）
- 缓冲-刷新分离设计：`L/R_ws2812_rgb()` 只写内存"显存"数组，`L/R_ws2812_refresh()` 统一发送，6 颗灯同步更新

### 3. 串口命令灯效扩展（任务19 样例基础上的自主改造）

- **命令链路**：串口中断收字节 → `USART_RX_STA` 信箱标志置位 → 主循环取命令、清标志
- **命令识别**：逐字节前缀匹配状态机，支持命令 `HELLO` / `ALLON` / `BLINK` / `RAIN` / `FLOW` / `OFF`
- **非阻塞灯效改造**：原版 `L_runingled()` 内含 `while(1)` 死循环会堵死串口接收；改造后每个灯效函数每次调用只播**一帧**，动画进度用 `static` 变量跨调用记忆，主循环按当前模式分发——新命令随时可打断切换
- **新增函数**：`fx_hello_frame()`（前后流光）、`fx_blink_frame()`（12 颗同步闪烁）、`fx_rain_frame()`（雨滴换色）、`fx_flow_frame()`（流水扫描）、`led_all_on()` / `led_all_off()`

## motor_pwm 工程（对应任务21）

### 4. TIM4 PWM 电机驱动

- 自写 `QST_HARDWARE/motor/motor.c/.h` 三函数模块：`Motor_Init()`（PB13/PB14 方向脚推挽输出）、`PWM_Init(arr,psc)`（PB6/PB7 复用推挽交权 TIM4，ARR=7199/PSC=9 → 1kHz）、`Set_Pwm(moto1,moto2)`（符号定方向、绝对值定占空比，反转时比较值反相 `7199-|x|`）
- 引脚映射：右电机 PWM=PB6(TIM4_CH1)/方向=PB13，左电机 PWM=PB7(TIM4_CH2)/方向=PB14
- `main.c` 电机版：框架初始化 → `PWM_Init(7199,9)` → 主循环 `Set_Pwm(2500,2500)`（约 35% 占空比双轮前进）
- 工程接线：uvprojx 手工加入 motor.c（HARDWARE 分组）与 `..\QST_HARDWARE\motor` 头文件路径；命令行编译 `"C:\Keil_v5\UV4\UV4.exe" -b Template.uvprojx -j0 -o build_log.txt` 0 Error
- 8-26 实机验证：架空开电池，双轮同速转动 ✅

### 5. TIM2/TIM3 编码器测速（任务22）

- 自写 `QST_HARDWARE/encoder/encoder.c/.h`：TIM2（左轮 PA0/PA1）、TIM3（右轮 PA6/PA7）设为编码器接口模式，A/B 相正交解码 4 倍频，CNT 随轮子正反转增减
- `stm32f10x_it.c` 填官方留空的 `SysTick_Handler`：100ms 节拍读两轮 CNT 差值，串口打印脉冲速度（本模板 delay_ms 是纯软件空转，SysTick 归测速用）
- 工程合并任务21 电机驱动：`Set_Pwm(2500,2500)` 双轮常转 + 编码器同时打印 = 任务23 的合体形态
- 参数标定：P=2800 脉冲/圈（官方标称 ppr700 × 4 倍频，右轮实测 2739 印证），轮周长 0.1425m → 1 米 = 19649 脉冲

### 6. 增量式 PID 双轮闭环 + 行程状态机（任务23）

- **内环**：增量式 PID 速度闭环，结构体化双轮各一套参数；每 100ms「目标速度 vs 编码器实测 → 增量修 PWM」。绕开参考答案三坑：积分项算了没接入输出、KP/KD 角色与标准公式对调、TageB=-1.0 暗示右编码器方向相反
- **外环纠偏**（`YAW_CORRECT` 开关）：两轮里程差累计 = 偏航角（差速里程计原理），修正量差动叠加到两轮目标速度把里程差追平；修正限幅防振荡。注意外环保航向不保横线（S 形纠偏下的横向位移它看不见）
- **软启动**：`RAMP_STEP=20`，目标速度每拍只爬 20，克静摩擦死区的起步挣脱；换向时目标扫过 0 = 自动软刹车
- **行程状态机（任务模式）**：前进 1 米 → 倒退 1 米回起点，平均里程 19649 脉冲判距，全程外环纠偏，终点 odo/diff 双归零 = 位置和朝向都回来
- 8-27 实机：架空 ✅（终点 odo/diff ≈3 脉冲）；落地首跑因起步挣脱不对称横向偏移，加软启动后平地复跑 ✅。三档速度作业 140/280/420 轮换验证（140 档静摩擦死区极限环为物理现象）

### 7. 历次提交附带的仓库维护

- 新增 `README.md`（GitHub 展示页），`.gitignore` 移除 `/README.md` 排除规则使其可入库
- `学习记录.md` 8-26 新增第 8~10 条（互斥锁 vs 线程调度、库函数分层、PWM 调压原理）
- 8-27：`.gitignore` 新增 `build_log.txt` 排除（Keil 命令行编译日志）；`学习记录.md` 8-26 新增第 11~15 条（差速里程计、串级闭环、定时器与通道、PWM 比较器、tick）、8-27 新增第 1~7 条（UartRead 阻塞读实锤、JDY-16 AT 指令、串口编码三坑、静摩擦死区极限环、航向保持、队友直线启示、超 5 米直线误差分类）

---
最近更新：2026-08-27
